##################################################################################
# Amazon Web Services Provider
##################################################################################

provider "aws" {
  #profile   = "lino-tf" /* You can use an AWS Profile instead using AWS Access and Secret Keys. */
  region    = "us-east-1"
}